tpch:
  target: dev
  outputs:
    dev:
      type: spark
      method: thrift
      host: localhost
      port: 10001
      schema: tpch_mart
      connect_retries: 5
      connect_timeout: 60
      threads: 1
